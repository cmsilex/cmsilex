{% extends 'base.html.twig' %}

{% block title %}{{ page.title }}{% endblock title %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{ page.title }}</h1>
                <hr>
                {{ app.textile.textileThis(page.content)|raw }}
            </div>
        </div>
    </div>
{% endblock body %}