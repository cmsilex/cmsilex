{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>CMSilex</h1>
        <p>
            <a href="{{ url('list_pages') }}">Pages</a>
        </p>
        <p>
            <a href="{{ url('list_posts') }}">Posts</a>
        </p>
        <p>
            <a href="{{ url('list_menus') }}">Menus</a>
        </p>
        <p>
            <a href="{{ url('media') }}">Media</a>
        </p>
        <p>
            <a href="{{ url('settings') }}">Site Settings</a>
        </p>
    </div>
{% endblock body %}